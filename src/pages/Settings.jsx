import { useState } from 'react'; import { FolderOpen, RefreshCw, UserPlus, LogIn, Trash2, Check } from 'lucide-react'; import { useEternalStore } from '../store/useEternalStore.js'; import { call,api } from '../lib/api.js';
export default function Settings(){const settings=useEternalStore(s=>s.settings)||{};const patch=useEternalStore(s=>s.patchSettings);const accounts=useEternalStore(s=>s.accounts);const active=useEternalStore(s=>s.activeAccountId);const refresh=useEternalStore(s=>s.refreshAccounts);const[offline,setOffline]=useState('');const[java,setJava]=useState([]);return <div><div className="page-head"><div><small>SETTINGS</small><h1>Control room</h1><p>Launcher paths, Java, memory, accounts and real integration configuration.</p></div></div><div className="settings-grid"><section className="settings-card"><h3>Accounts</h3><div className="account-list">{accounts.map(a=><div className={`account-row ${a.id===active?'selected':''}`} key={a.id}><div className="avatar">{a.username[0]}</div><div><b>{a.username}</b><span>{a.type==='microsoft'?'Microsoft / Minecraft':'Offline'}</span></div>{a.id===active?<Check/>:<button onClick={async()=>{await call(api.accounts.activate(a.id));refresh()}}>Use</button>}<button className="icon-btn" onClick={async()=>{await call(api.accounts.remove(a.id));refresh()}}><Trash2/></button></div>)}</div><div className="inline-form"><input value={offline} onChange={e=>setOffline(e.target.value)} placeholder="Offline username"/><button onClick={async()=>{await call(api.accounts.addOffline(offline));setOffline('');refresh()}}><UserPlus/>Add</button></div><button className="primary wide" onClick={async()=>{try{await call(api.accounts.loginMicrosoft());await refresh()}catch(e){alert(e.message)}}}><LogIn/>Sign in with Microsoft</button></section><section className="settings-card"><h3>Microsoft application</h3><label>Azure / Entra public client ID<input value={settings.azureClientId||''} onChange={e=>patch({azureClientId:e.target.value})} placeholder="Your own application client ID"/></label><small className="muted">Eternal never copies another launcher's client ID. Enable public client/device-code flows on your own app registration.</small></section><section className="settings-card"><h3>Java</h3><label>Java executable<input value={settings.javaPath||''} onChange={e=>patch({javaPath:e.target.value})} placeholder="Auto detect if empty"/></label><button className="secondary wide" onClick={async()=>setJava(await call(api.java.detect()))}><RefreshCw/>Detect Java</button>{java.map(j=><button className="java-row" key={j.path} onClick={()=>patch({javaPath:j.path})}><b>Java {j.major}</b><span>{j.path}</span></button>)}</section><section className="settings-card"><h3>Game</h3><label>Default memory<div className="range-line"><input type="range" min="2048" max="16384" step="512" value={settings.ramMb||6144} onChange={e=>patch({ramMb:Number(e.target.value)})}/><b>{((settings.ramMb||6144)/1024).toFixed(1)} GB</b></div></label><div className="form-row"><label>Width<input type="number" value={settings.resolution?.width||1280} onChange={e=>patch({resolution:{...settings.resolution,width:Number(e.target.value)}})}/></label><label>Height<input type="number" value={settings.resolution?.height||720} onChange={e=>patch({resolution:{...settings.resolution,height:Number(e.target.value)}})}/></label></div></section><section className="settings-card"><h3>Storage</h3><label>Eternal data folder<input value={settings.dataDir||''} readOnly placeholder="Default Electron user data"/></label><button className="secondary wide" onClick={async()=>{const p=await call(api.dialog.folder());if(p)patch({dataDir:p})}}><FolderOpen/>Choose folder</button><small className="muted">Changing this setting affects newly resolved paths after restart. Existing data is not silently deleted.</small></section></div></div>}
