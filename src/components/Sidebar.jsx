import { NavLink } from 'react-router-dom'; import { Home, Boxes, Puzzle, Server, Gem, Settings, MessageCircle } from 'lucide-react'; import { useEternalStore } from '../store/useEternalStore.js';
const links=[['/',Home,'Home'],['/library',Boxes,'Instances'],['/mods',Puzzle,'Mod Hub'],['/servers',Server,'Servers'],['/core',Gem,'Eternal Core']];
export default function Sidebar(){const accounts=useEternalStore(s=>s.accounts);const activeId=useEternalStore(s=>s.activeAccountId);const a=accounts.find(x=>x.id===activeId);return <aside className="sidebar"><div className="logo-orb"><img src="/assets/logo.svg"/></div><nav>{links.map(([to,I,label])=><NavLink key={to} to={to} end={to==='/' } data-tip={label} className={({isActive})=>isActive?'nav-icon active':'nav-icon'}><I size={20}/></NavLink>)}</nav><div className="sidebar-spacer"/><button className="nav-icon" data-tip="Discord" onClick={()=>window.eternal.app.openExternal('https://discord.gg/YOUR_INVITE_HERE')}><MessageCircle size={20}/></button><NavLink to="/settings" data-tip="Settings" className={({isActive})=>isActive?'nav-icon active':'nav-icon'}><Settings size={20}/></NavLink><div className="account-dot" title={a?.username||'No account'}>{a?.skinUrl?<img src={a.skinUrl}/>:<span>{(a?.username||'?')[0].toUpperCase()}</span>}<i className={a?'online':''}/></div></aside>}
